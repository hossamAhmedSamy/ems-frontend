# Frontend env. Copy to .env.local for overrides. The Vite dev server
# proxies /api → http://localhost:3000 (see vite.config.ts), so VITE_API_URL
# can stay as the default below in development.

VITE_API_URL=/api
VITE_APP_NAME=EMS v2
